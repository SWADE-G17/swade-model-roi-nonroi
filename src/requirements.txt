# ============================================================
# requirements.txt
# Proyecto: Alzheimer Detection - 3D ResNet + CBAM
# ============================================================
# Instalacion (Windows con Anaconda):
#   conda activate alzheimer
#   pip install -r requirements.txt
#
# Instalacion (Mac M4):
#   conda activate alzheimer
#   pip install tensorflow-macos tensorflow-metal
#   pip install -r requirements.txt --ignore-installed tensorflow
# ============================================================

# Deep Learning
tensorflow==2.20.0          # GPU nativa en Windows. En Mac M4: usar tensorflow-macos

# Neuroimagen
nibabel>=4.0.0              # Leer archivos .mgz, .nii, .nii.gz

# Procesamiento de imagenes
scikit-image>=0.19.0        # resize, unsharp_mask
opencv-python>=4.6.0        # CLAHE (cv2.createCLAHE)
scipy>=1.9.0                # scipy.ndimage.rotate (augmentacion)

# Registro de imagenes (definido en el paper, no activo en entrenamiento)
dipy>=1.5.0

# Machine Learning / Datos
scikit-learn>=1.1.0         # train_test_split, compute_class_weight, metrics
numpy>=1.21.0
pandas>=1.4.0

# Visualizacion
matplotlib>=3.5.0
seaborn>=0.12.0

# Jupyter (para correr los notebooks originales)
jupyter>=1.0.0
ipykernel>=6.0.0

# Worker service (RabbitMQ consumer + MinIO + Supabase)
pika>=1.3.0                 # RabbitMQ client (AMQP 0-9-1)
javaobj-py3>=0.4.4          # Deserialize Java-serialized RabbitMQ payloads
minio>=7.2.0                # MinIO / S3-compatible object storage client
httpx>=0.27.0               # HTTP client for Supabase PostgREST API
python-dotenv>=1.0.0        # Load .env files into os.environ
